# Grafana Dashboard

A simple Grafana dashboard for OpenErgo usage, credit, and pain metrics. Enable
OpenTelemetry export to a Prometheus data source to use this.

![OpenErgo Summary dashboard screenshot](screenshot.png)

## Import

Import `summary.json` in Grafana to add the **OpenErgo Summary** dashboard.